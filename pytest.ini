[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
# Keep PVcopilot's noisy filter/print output from drowning the test report.
filterwarnings =
    ignore::DeprecationWarning
