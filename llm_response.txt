# Import necessary packages
import pandas as pd
import numpy as np

# Load data as df; change file path if necessary
df = pd.read_parquet('sys_1278_downsampled.parquet')

# Define a mapping dictionary for variable normalization; fill as needed
mapped_variables_dict = {}

# Normalize the dataframe using provided function
df_filtered = normalize(df, mapped_variables_dict)

# List of selected filters; adjust as necessary
selected_filters = ['timezone', 'low-irra-power', 'outlier']

# Initialize lists to hold normal and outlier indices
normal_idx_list = []

# Apply low irradiation power filter if selected
if "low-irra-power" in selected_filters:
    normal_idx_lip, outlier_idx_lip = low_irra_power_filter(df_filtered, mapped_variables_dict)
    normal_idx_list.append(set(normal_idx_lip))

# Apply IQR outlier detection filter if selected
if "outlier" in selected_filters:
    normal_idx_iqr, outlier_idx_iqr = identify_outliers_iqr(df_filtered, "norm")
    normal_idx_list.append(set(normal_idx_iqr))

# Merge all normal indices (intersection)
if normal_idx_list:
    normal_idx = set.intersection(*normal_idx_list)
else:
    normal_idx = set(df_filtered.index)

# Calculate and print counts
total_points = len(df_filtered)
num_normal = len(normal_idx)
num_outliers = total_points - num_normal
print(f"Total points: {total_points}, Normal: {num_normal}, Outliers: {num_outliers}")

# Filter the dataframe to only keep normal data points
df_filtered_final = df_filtered.loc[list(normal_idx)]

# Define the irradiation key for daily aggregation; adjust key name as necessary
irra_key = 'irra' # <- Change to actual irradiation column name

# Aggregate data daily
daily_data = aggregate_daily(df_filtered_final, irra_key)

# Compute year-over-year distribution and relative difference
rd, yoy_dist = compute_yoy(daily_data)

# Print relative difference
print(rd)