{
  "packages_code": "import pandas as pd\nimport numpy as np",
  "main_code": "# Main code\n# Load data (provide full file path if needed)\ndf = pd.read_parquet('sys_1403_part1_downsampled.parquet')\n\n# Define variable mapping dictionary\nmapped_variables_dict = {'DC Power': 'dc_power', 'AC Power': 'ac_power', 'Irradiance': 'poa_irradiance', 'Module temperature': 'module_temperature_C', 'Time': '__index__'}\n\n# Normalize dataframe\n[df_filtered] = [normalize(df, mapped_variables_dict)]\n\n# Define filters\nselected_filters = ['timezone', 'low-irra-power', 'outlier']\n\nnormal_idx_sets = []\n\n# Apply low irradiance power filter\nif 'low-irra-power' in selected_filters:\n    normal_idx, outlier_idx = low_irra_power_filter(df_filtered, mapped_variables_dict)\n    normal_idx_sets.append(normal_idx)\n\n# Apply outlier filter\nif 'outlier' in selected_filters:\n    normal_idx_out, outlier_idx_out = identify_outliers_iqr(df_filtered, 'norm')\n    normal_idx_sets.append(normal_idx_out)\n\n# Merge all normal indices\nif normal_idx_sets:\n    normal_idx = set(normal_idx_sets[0])\n    for idx_set in normal_idx_sets[1:]:\n        normal_idx = normal_idx.intersection(idx_set)\n    normal_idx = list(normal_idx)\nelse:\n    normal_idx = df_filtered.index.tolist()\n\n# Calculate outliers as points not in normal_idx\ntotal_points = len(df_filtered)\nnum_normal = len(normal_idx)\nnum_outliers = total_points - num_normal\n\nprint(f'Total number of points: {total_points}')\nprint(f'Number of normal points: {num_normal}')\nprint(f'Number of outliers: {num_outliers}')\n\n# Filter dataframe to normal points only\ndf_filtered_final = df_filtered.loc[normal_idx]\n\n# Aggregate daily data using irradiance key\nirra_key = mapped_variables_dict['Irradiance']\ndaily_data = aggregate_daily(df_filtered_final, irra_key)\n\n# Define selected metrics\nselected_metric = ['YOY']\n\n# Compute metrics based on selection\nif 'YOY' in selected_metric:\n    rd, _ = compute_yoy(daily_data)\nelif 'LR' in selected_metric:\n    rd, _ = compute_lr(daily_data)\nelif 'ARIMA' in selected_metric:\n    rd, _ = compute_arima(daily_data)\nelif 'CSD' in selected_metric:\n    rd, _ = compute_csd(daily_data)\nelif 'HW' in selected_metric:\n    rd, _ = compute_hw(daily_data)\n\n# Print results\nprint(rd)"
}