{
  "packages_code": "import pandas as pd\nimport numpy as np",
  "main_code": "# Main code\n# Load data - user may need to provide full file path\ndf = pd.read_parquet('sys_1278_downsampled.parquet')\n\n# Define variable mapping dictionary\nmapped_variables_dict = {'DC Power': 'dc_power', 'AC Power': 'ac_power', 'Irradiance': 'poa_irradiance', 'Module temperature': 'module_temperature_C', 'Time': '__index__'}\n\n# Normalize dataframe\n[df_filtered] = normalize(df, mapped_variables_dict)\n\n# Define filters\nselected_filters = ['timezone', 'low-irra-power', 'outlier', 'clearsky']\n\n# Initialize lists to gather indices\nnormal_indices_list = []\noutlier_indices_list = []\n\n# Apply low-irra-power filter if selected\nif 'low-irra-power' in selected_filters:\n    normal_idx, outlier_idx = low_irra_power_filter(df_filtered, mapped_variables_dict)\n    normal_indices_list.append(normal_idx)\n    outlier_indices_list.append(outlier_idx)\n\n# Apply outlier filter if selected\nif 'outlier' in selected_filters:\n    normal_idx, outlier_idx = identify_outliers_iqr(df_filtered, 'norm')\n    normal_indices_list.append(normal_idx)\n    outlier_indices_list.append(outlier_idx)\n\n# Merge all normal indices using set intersection\nif normal_indices_list:\n    normal_idx = set(normal_indices_list[0])\n    for idx in normal_indices_list[1:]:\n        normal_idx = normal_idx.intersection(set(idx))\n    normal_idx = list(normal_idx)\nelse:\n    normal_idx = df_filtered.index.tolist()\n\n# Merge all outlier indices using set union\nif outlier_indices_list:\n    outlier_idx = set().union(*outlier_indices_list)\n    outlier_idx = list(outlier_idx)\nelse:\n    outlier_idx = []\n\n# Print counts\nprint(f\"Total points: {len(df_filtered)}\")\nprint(f\"Normal points: {len(normal_idx)}\")\nprint(f\"Outliers: {len(outlier_idx)}\")\n\n# Filter dataframe to keep only normal points\ndf_filtered_final = df_filtered.loc[normal_idx]\n\n# Aggregate daily data\nirra_key = 'poa_irradiance'\ndaily_data = aggregate_daily(df_filtered_final, irra_key)\n\n# Select metric\nselected_metric = ['HW']\n\n# Compute depending on selected metric\nif 'YOY' in selected_metric:\n    rd, _ = compute_yoy(daily_data)\nelif 'LR' in selected_metric:\n    rd, _ = compute_lr(daily_data)\nelif 'ARIMA' in selected_metric:\n    rd, _ = compute_arima(daily_data)\nelif 'CSD' in selected_metric:\n    rd, _ = compute_csd(daily_data)\nelif 'HW' in selected_metric:\n    rd, _ = compute_hw(daily_data)\n\n# Print results\nprint(rd)"
}